<python 궁금증>
-python의 메인함수

<python 궁금증>
(chap 1)
-왜 느린가 (C 언어의 10~350배)


import 모듈 - > python 환경변수 -> PYTHONPATH뒤져서 모듈을 가져옴

-파라미터 아규먼트의 차이

-문자열과 리스트의 차이?
*아예 다른 자료형이다. 문자열 범위지정은 


<python 궁금증>
(chap 2)
-매개변수의 뜻
-powershell 작동되는 원리
-cmd 작동되는 원리

-문자열 저장되는 원리
-어떻게 배열이 음수가 들어가서
작동되는 가
-어떻게 범위 설정이 되는가


-문자열에서 숫자로 어떻게 변환하는가

-+= 연산자는 어떻게 만들어졌을까


-문자열에서 숫자로 어떻게 변환하는가
-format() 함수에서 "{}."은 무슨 의미일까

-print()만 써도 행 바꿈??
  A.print(.... end="") 를 써주면 행이 안 바뀌긴 한다.


-strip() 으로 문자 없애기
-rfind() 진행과정
-split 함수 물어보기

-왜 문자열 범위 선택 연산자는 y-1번 방 까지인가
*0번부터 0번까지라 하면 범위 지정에 애매함이 생긴다. 확실히 하게 하기 위해


<python 궁금증>
(chap 3)

-if 조건문에서 왜 콜론이 붙을까\
*들여쓰기가 되지 않고 error가 뜬다

-datetime 함수(?) 이용
 A.datetime.datetime.now() 는 아마도 datetime 안에 있는 클라스 안에 datetime.now()라는 함수를 할당해주는 것일 것이다.

-datetime이 정보를 가지고 오는 경로, 진행순서?

-else if 가 아니고 elif
-절대시간ㅊvs 현재시간

-왜 python 논리연산자에는 xor는 없을까
   A. '^' 라는 비트 연산자를 쓰지 않을까요 
    
-문자열 자체는 리스트가 되는 것인가
-리스트 메모리 저장 과정
*컴파일러 인터프리터 차이점

-python 의 변수에 'None' 이 나오는 것은?
-요소 제거에서 (del) 범위 지정 요소 삭제는
왜 [3:]일떄 한개 크게 잡고 삭제할까?

-클래스나 함수들은 어떻게 만들어지는 걸까?

-dictionary접근 원리
-왜 키와 값인가?